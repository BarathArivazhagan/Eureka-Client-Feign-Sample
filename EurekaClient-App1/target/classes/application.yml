

spring:
  application:
    name: eureka-client-app1

server:
  port: 8082 

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka
    fetch-registry: true
    register-with-eureka: true
    
   